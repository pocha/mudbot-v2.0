{
  "name": "mudbot-v2",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "functions",
    "extension",
    "scripts"
  ],
  "scripts": {
    "emulators": "firebase emulators:start --only functions,firestore",
    "build": "npm run build --workspaces --if-present",
    "seed-conversation": "npm run seed-conversation --workspace scripts --"
  }
}
