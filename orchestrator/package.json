{
  "name": "orchestrator",
  "private": true,
  "version": "0.1.0",
  "main": "lib/index.js",
  "engines": { "node": "22" },
  "scripts": {
    "postinstall": "node scripts/setup-docker.js",
    "build": "tsc",
    "start": "node lib/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "firebase": "^11.0.2",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^5.0.0"
  }
}
